{% extends 'sites/udemyPersos/basePersos.html.twig' %}

{% block title %}TP Personnage{% endblock %}

{% block monTitre %}Mon {{ arme.nom }}{% endblock %}

{% block body %}
<div class="row">
    <div class="col-2">
        <img src="{{ asset('images/projects/armes/' ~ arme.nom|replace({'é':'e'}) ~ '.png') }}" alt="">
    </div>
    <div class="col">
        <h2><div>{{ arme.nom}}</div></h2>
        <div>{{ arme.descriptif }}</div>
        <div>{{ arme.degats }}</div>        
    </div>
</div>
{% endblock %}